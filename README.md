
  # Distribute Module v3 Design

  This is a code bundle for Distribute Module v3 Design. The original project is available at https://www.figma.com/design/woUsKTnuCPs0jYNs3bX8TH/Distribute-Module-v3-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  