# Fast Create Drop - AUDIENCE & AUTH 设计说明

## 🎨 设计理念

Fast Create Drop 页面采用简洁、快速的设计风格，与 Settings 中的详细配置界面明确区分：

- **Settings** = 详细配置、管理、编辑
- **Fast Create Drop** = 快速选择、简洁展示、专注创建

## 📋 设计对比

### Settings → Access Policies
```
✓ 详细的卡片布局
✓ Hover 显示操作按钮（Edit, Duplicate, Delete）
✓ 完整的配置选项
✓ 适合管理和编辑
```

### Fast Create Drop → AUDIENCE & AUTH
```
✓ 简洁的列表布局
✓ 聚焦于选择而非编辑
✓ 关键信息展示
✓ 快速预览配置
```

## 🎯 新设计特点

### 1. Standard Policies

#### 视觉设计
- **圆形图标**：使用圆形背景（而非方形），更柔和
- **单选按钮**：位于右侧，清晰的选择指示
- **简洁布局**：图标 + 标题 + 描述 一体化展示
- **悬停效果**：边框高亮，提供视觉反馈

#### 交互设计
- **点击卡片**：选中策略
- **View Configuration**：可折叠的详细配置
- **选中状态**：蓝色边框 + 浅蓝背景

#### 信息层级
```
Primary: 策略名称 + Default 徽章
Secondary: 标语（Maximum Reach & Frictionless Access）
Tertiary: 描述（Eliminate all barriers...）
Hidden (默认): 详细配置（可展开查看）
```

### 2. Custom Policies

#### 创新的选择器设计
采用独特的"徽章 + 下拉"设计：

```
┌────────────────────────────────────┐
│ 🟢  [11]                          │  ← 数量徽章
│     ↓ 选择一个自定义策略...        │  ← 下拉提示
└────────────────────────────────────┘

选中后：
┌────────────────────────────────────┐
│ 🟢  [11]                          │
│     My Test Policy                │  ← 策略名称
│     Based on "Lead Capture"       │  ← 来源信息
│                                    │
│     ───────────────────────────   │  ← 分隔线
│     Required Fields:  email       │  ← 详细配置
│     Content Levels:   Public...   │
│     Download Policy:  Guarded     │
│     Watermark:        Disabled    │
│     Link Validity:    90 days     │
└────────────────────────────────────┘
```

#### 设计优势
- **一目了然**：通过数量徽章知道有多少自定义策略
- **节省空间**：折叠式设计，不占用过多空间
- **清晰展示**：选中后自动展开显示详细信息
- **区分明显**：绿色主题区别于标准策略的蓝色

### 3. 引导链接

#### 设计改进
- **可点击按钮**：从纯展示升级为可点击
- **直接跳转**：点击后直接导航到 Settings
- **视觉提示**：蓝色背景 + 悬停效果

```javascript
onClick={() => onNavigate('settings')}
```

## 🎨 颜色方案

### Standard Policies 选中状态
```css
border: primary (蓝色)
background: blue-50/50 (淡蓝色)
```

### Custom Policies 选中状态
```css
border: primary (蓝色)
background: emerald-50/50 (淡绿色)
icon-bg: emerald-100
icon-color: emerald-600
```

### 引导链接
```css
background: blue-50
border: blue-200
text: blue-900
hover: blue-100 / blue-300
```

## 📐 布局规范

### 图标尺寸
- **Standard Policies**: 40px × 40px (圆形)
- **Custom Policies**: 40px × 40px (圆形)
- **下拉选项图标**: 24px × 24px (圆形)

### 间距
- **卡片内边距**: 12px (p-3)
- **图标与文字间距**: 12px (gap-3)
- **卡片间距**: 8px (space-y-2)

### 字体大小
- **策略名称**: text-sm (14px)
- **标语**: text-xs (12px)
- **描述**: text-xs (12px)
- **配置详情**: text-xs (12px)

## 🔄 交互流程

### Standard Policy 选择流程
```
1. 用户点击策略卡片
   → 选中状态激活（蓝色边框 + 背景）
   → 右侧单选按钮填充
   → 预览面板更新

2. 用户点击 "View Configuration"
   → 展开详细配置
   → 显示 Access Mode, Fields, Download Policy 等
   → 按钮图标变为 ChevronUp

3. 用户再次点击 "View Configuration"
   → 折叠配置
   → 按钮图标变为 ChevronDown
```

### Custom Policy 选择流程
```
1. 用户点击下拉选择器
   → 显示所有自定义策略列表
   → 每个选项显示图标 + 名称 + 来源

2. 用户选择一个策略
   → 选择器显示选中策略名称
   → 自动展开详细配置
   → 预览面板更新（绿色主题）

3. 配置自动展示
   → Required Fields
   → Content Levels
   → Download Policy
   → Watermark
   → Link Validity
```

### 导航到 Settings 流程
```
1. 用户点击底部引导按钮
   → onNavigate('settings') 触发
   → 页面切换到 Settings
   → 自动定位到 Access Policies 标签
```

## 💡 设计原则

### 1. 简洁优先
- 移除不必要的视觉元素
- 保留核心信息
- 使用留白创造呼吸感

### 2. 快速识别
- 圆形图标易于识别
- 颜色编码区分类型
- 清晰的选中状态

### 3. 渐进展示
- 默认显示关键信息
- 详细配置按需展开
- 避免信息过载

### 4. 一致性
- 与 Material Pool Selector 风格一致
- 与 Template Picker 风格一致
- 保持整体页面和谐

## 🎯 用户体验目标

### 效率
- **3 秒内**完成策略选择
- **1 次点击**查看详细配置
- **直接跳转**到管理页面

### 清晰
- **一眼识别**标准策略
- **数量徽章**显示自定义策略数量
- **颜色区分**不同策略类型

### 引导
- **空状态**提示创建自定义策略
- **底部按钮**引导到管理页面
- **提示文字**说明如何操作

## 📊 对比总结

| 特性 | Settings 设计 | Fast Create Drop 设计 |
|------|--------------|---------------------|
| 目的 | 管理和编辑 | 快速选择 |
| 布局 | 3 列网格 | 单列列表 |
| 图标 | 方形（10×10） | 圆形（10×10） |
| 操作按钮 | Edit, Duplicate, Delete | View Configuration only |
| Custom Policies | 独立卡片展示 | 下拉选择器 + 徽章 |
| 空状态 | 提示信息 | 引导按钮 |
| 主题色 | 多种颜色 | 蓝色 + 绿色 |

## 🚀 技术实现

### 关键组件
```typescript
// Standard Policies: Collapsible 卡片
<Collapsible>
  <div onClick={handleSelectStandardPolicy}>
    <圆形图标 + 文字 + 单选按钮>
    <CollapsibleTrigger>View Configuration</CollapsibleTrigger>
  </div>
  <CollapsibleContent>
    <配置详情>
  </CollapsibleContent>
</Collapsible>

// Custom Policies: Select + Collapsible
<Collapsible>
  <Select onValueChange={handleSelectCustomPolicy}>
    <圆形图标 + 徽章 + 下拉>
  </Select>
  <CollapsibleContent>
    <配置详情>
  </CollapsibleContent>
</Collapsible>

// 引导按钮
<Button onClick={() => onNavigate('settings')}>
  <图标 + 引导文字>
</Button>
```

### 状态管理
```typescript
const [policySource, setPolicySource] = useState<'standard' | 'custom'>('standard');
const [selectedPolicy, setSelectedPolicy] = useState<PolicyType>('lead-capture');
const [selectedCustomPolicyId, setSelectedCustomPolicyId] = useState<string | null>(null);
const [expandedPolicyId, setExpandedPolicyId] = useState<PolicyType | null>(null);
```

## 📝 使用建议

### 创建 Drop 时
1. 先选择 Material Pool
2. 再选择 Scene Template
3. 最后选择 Access Policy（默认 Lead Capture）
4. 如需自定义配置，点击 "View Configuration" 预览
5. 如需创建新策略，点击底部按钮跳转到 Settings

### 日常使用
- 大部分情况使用 Standard Policies
- 特殊需求时使用 Custom Policies
- 定期在 Settings 中管理和清理自定义策略

---

**设计版本**: v3.2  
**更新日期**: 2025-10-16  
**设计师**: AI Assistant
